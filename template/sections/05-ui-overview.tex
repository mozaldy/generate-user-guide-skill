\section{Getting Around}

% Describe the overall layout: sidebar, main area, header. List the sidebar
% menu items in a small table. Use \ugMenu{} for each item so the inline
% screenshot renders if a PNG was captured.

The screen is divided into two parts. The menu on the left lets you move
between areas of the app. The main area on the right shows whatever you
are working on.

% \ugScreenshot[0.9]{screenshots/02-dashboard.png}{The home screen after sign-in}

\subsection{The left-hand menu}

\begin{center}
\begin{tabular}{p{3cm}p{9cm}}
  \toprule
  \textbf{Menu item} & \textbf{What it does} \\
  \midrule
  \ugMenu{Dashboard}    & Takes you back to the home screen \\
  % Add one row per sidebar nav item found during source exploration.
  \bottomrule
\end{tabular}
\end{center}

\begin{ugNote}
  Most pages also show a trail of links near the top (such as
  ``Section / Unit / Lesson''). Click any link in the trail to jump back
  to that level.
\end{ugNote}
