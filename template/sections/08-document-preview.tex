% ============================================================================
% Core Feature: Document Preview
% ============================================================================
\section{Document Preview}

\begin{ugModule}{Overview}

  \textbf{Purpose:}
  View documents directly in the browser without downloading.

  \textbf{Who Can Access:}
  \ugRole{Admin} \ugRole{Manager} \ugRole{Reviewer} \ugRole{Staff}
\end{ugModule}

\subsection{Key Functions}
\begin{itemize}[leftmargin=18pt]
  \item \textbf{In-Browser Preview} --- View the full PDF
    directly in your browser using the built-in PDF viewer
  \item \textbf{HTTP Range Streaming} --- Large documents are
    streamed progressively (HTTP~206 Partial Content) for
    fast initial display without downloading the entire file
  \item \textbf{Version Preview} --- Preview any specific
    version, not just the latest
\end{itemize}

\subsection{Step-by-Step: Previewing a Document}
\begin{ugSteps}
  \item Navigate to the document detail page.
  \item Click the \ugButton{Preview} button.
  \item The PDF opens in the browser's built-in viewer.
  \item To preview an older version, select the desired version
        from the \ugField{Version} dropdown, then click
        \ugButton{Preview}.
\end{ugSteps}

\subsection{Preview vs.\ Download}

\begin{tabularx}{\textwidth}{@{} l X X @{}}
  \toprule
  \textbf{Aspect} & \textbf{Preview}
    & \textbf{Download} \\
  \midrule
  Format
    & Original vector PDF
    & Rasterized (image-based) PDF \\
  Watermark
    & None
    & Embedded on every page \\
  Text Selection
    & Yes (native PDF)
    & No (pages are images) \\
  Offline Access
    & No (streaming)
    & Yes (file saved locally) \\
  \bottomrule
\end{tabularx}

\subsection{Business Rules}
\begin{itemize}[leftmargin=18pt]
  \item Preview serves the original PDF with
        \texttt{Content-Disposition: inline}.
  \item Access control (ACL) is enforced --- only authorized
        users can preview a document.
  \item Expired documents (past \ugField{Expiry Date}) cannot
        be previewed.
  \item Preview does not count toward the download rate limit.
\end{itemize}

\begin{ugTip}
  Use Preview to review document content quickly. Only use
  Download when you need an offline copy --- downloads are
  watermarked and rate-limited.
\end{ugTip}

\newpage
