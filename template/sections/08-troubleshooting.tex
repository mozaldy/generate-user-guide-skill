\section{Troubleshooting}

% 3--5 ugError rows: the error message users see, the cause, and the fix.
% Think about what goes wrong when someone fills a form wrong, uploads the
% wrong file type, hits a size limit, etc.

\begin{ugErrorTable}
  \ugError{The error message users see}
          {What went wrong (the cause)}
          {What the user should do (the fix)}
\end{ugErrorTable}
